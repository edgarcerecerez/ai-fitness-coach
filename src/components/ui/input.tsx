import * as React from "react"

import { cn } from "@/lib/utils"

/**
 * Renders a styled HTML input element with full support for standard input attributes and custom class names.
 *
 * Applies consistent utility-based styling for appearance, accessibility, and state feedback, while allowing further customization through the `className` prop and additional input attributes.
 *
 * @returns The rendered input element.
 */
function Input({ className, type, ...props }: React.ComponentProps<"input">) {
  return (
    <input
      type={type}
      data-slot="input"
      className={cn(
        // Increased height to h-11 (44px) for touch targets
        // text-base on mobile (16px prevents zoom), text-sm on desktop
        "file:text-foreground placeholder:text-muted-foreground selection:bg-primary selection:text-primary-foreground dark:bg-input/30 border-input flex h-11 w-full min-w-0 rounded-md border bg-transparent px-3 py-2 text-base shadow-xs transition-[color,box-shadow] outline-none file:inline-flex file:h-7 file:border-0 file:bg-transparent file:text-sm file:font-medium disabled:pointer-events-none disabled:cursor-not-allowed disabled:opacity-50 md:text-sm",
        "focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px]",
        "aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive",
        className
      )}
      {...props}
    />
  )
}

export { Input }
